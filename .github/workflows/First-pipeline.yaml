name: Myfirstwork flow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest

        steps:
        - name: checkout the code
          uses: actions/checkout@v4
          
        - name: this is a first pipeline
          run: "echo this is a first pipeline"

        - name: list te files
          run: ls
        - name: read the file
          run: cat README.md 